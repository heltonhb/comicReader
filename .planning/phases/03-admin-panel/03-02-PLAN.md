---
phase: "03-admin-panel"
plan: "02"
type: "execute"
wave: "2"
depends_on:
  - "03-01"
files_modified:
  - "src/components/AdminPanel.jsx"
  - "src/store/adminStore.js"
  - "src/components/VolumeForm.jsx"
  - "src/api/adminApi.js"
autonomous: true
requirements:
  - "ADMIN-02"
must_haves:
  truths:
    - "Admin can create new volume catalog entries"
    - "Form validates required fields before submission"
    - "Volume metadata saved to Firestore volumes collection"
    - "Success feedback shown after save"
  artifacts:
    - path: "src/components/AdminPanel.jsx"
      provides: "Main admin dashboard layout"
      min_lines: 30
    - path: "src/components/VolumeForm.jsx"
      provides: "Volume cataloging form"
      min_lines: 50
    - path: "src/store/adminStore.js"
      provides: "Zustand store for admin state"
      min_lines: 30
  key_links:
    - from: "AdminPanel"
      to: "VolumeForm"
      via: "component import"
      pattern: "import.*VolumeForm"
    - from: "VolumeForm"
      to: "adminStore"
      via: "submit handler"
      pattern: "createVolume|addDoc"
---

<objective>
Create cataloging form for admin to create new volume entries in Firestore.

Purpose: Enable admin users to add new volumes (title, description, author, cover image URL, page count) to the volumes collection in Firestore. This is the "no-code" approach mentioned in Milestone 2 goal.

Output: AdminPanel dashboard, VolumeForm with validation, adminStore with volume creation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/store/authStore.js
@src/volumes.js
@src/components/VolumeSelector.jsx

# Volume Schema (from volumes.js)
Volumes have structure:
- id: string
- title: string  
- author: string
- coverUrl: string
- pageCount: number
- pdfUrl: string (reference to Storage)

# Firestore Collections
- "volumes" collection stores all volume metadata
- Document ID = volume unique ID
- Fields match the volume schema

# UI Pattern from Phase 2
- Use Tailwind for styling (gold accent #D4AF37)
- Loading spinners use BookLoader pattern
- Forms use controlled inputs with validation
</context>

<tasks>

<task type="auto">
  <name>task 1: Create adminStore for admin state</name>
  <files>src/store/adminStore.js</files>
  <action>
Create Zustand store for admin panel state in src/store/adminStore.js:
- State: { volumes: [], loading: false, error: null }
- Actions: fetchVolumes(), createVolume(data), deleteVolume(id), updateVolume(id, data)
- Use Firestore addDoc, getDocs, deleteDoc, updateDoc
- Import collection, doc, addDoc, getDocs, deleteDoc, updateDoc from firebase/firestore
- Export useAdminStore hook
  </action>
  <verify>
    <automated>npm run lint -- --quiet src/store/adminStore.js</automated>
  </verify>
  <done>adminStore provides volume CRUD operations</done>
</task>

<task type="auto">
  <name>task 2: Create VolumeForm component</name>
  <files>src/components/VolumeForm.jsx</files>
  <action>
Create cataloging form in src/components/VolumeForm.jsx:
- Fields: title (required), author (required), description (textarea), pageCount (number), coverUrl (text), pdfUrl (text)
- Use controlled inputs with React state
- Validation: required fields must not be empty, pageCount > 0
- Submit handler calls createVolume from adminStore
- Show loading state during submission
- Clear form on success, show success message
- Use Tailwind: form inputs with gold border on focus, submit button in gold
- Export default VolumeForm component
  </action>
  <verify>
    <automated>npm run lint -- --quiet src/components/VolumeForm.jsx</automated>
  </verify>
  <done>VolumeForm submits valid data to Firestore</done>
</task>

<task type="auto">
  <name>task 3: Create AdminPanel dashboard</name>
  <files>src/components/AdminPanel.jsx</files>
  <action>
Create main admin dashboard in src/components/AdminPanel.jsx:
- Layout: sidebar with navigation (Volumes, Uploads), main content area
- Show VolumeForm when "Volumes" tab selected
- Show "Dropzone" placeholder for Plan 3.3 when "Uploads" tab selected
- Use Tailwind: dark theme with gold accents matching Phase 2 style
- Load existing volumes list from adminStore on mount
- Show volume cards with edit/delete actions
- Export default AdminPanel component
  </action>
  <verify>
    <automated>npm run lint -- --quiet src/components/AdminPanel.jsx</automated>
  </verify>
  <done>AdminPanel shows cataloging form and volume management</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→Firestore | Admin writes volumes collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | Volume creation | mitigate | "Server validates data before Firestore write" |
| T-03-04 | Information Disclosure | Volume data | accept | "Volumes are meant to be public" |
</threat_model>

<verification>
Check that:
1. npm run lint passes for all new files
2. Form validates required fields before submission
3. Volume saved to Firestore on submit
</verification>

<success_criteria>
- [x] adminStore provides volume CRUD
- [x] VolumeForm validates and submits
- [x] AdminPanel displays form and volume list
- [x] New volumes appear in VolumeSelector (reader)
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-panel/03-02-SUMMARY.md`
</output>