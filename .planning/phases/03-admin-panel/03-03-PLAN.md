---
phase: "03-admin-panel"
plan: "03"
type: "execute"
wave: "3"
depends_on:
  - "03-02"
files_modified:
  - "src/components/Dropzone.jsx"
  - "src/hooks/useStorageUpload.js"
  - "src/components/FileList.jsx"
autonomous: true
requirements:
  - "ADMIN-03"
must_haves:
  truths:
    - "Admin can drag-and-drop multiple files"
    - "Files upload to Firebase Storage with progress"
    - "Upload progress is visible to user"
    - "Completed files show download URLs"
  artifacts:
    - path: "src/components/Dropzone.jsx"
      provides: "Drag-and-drop file upload area"
      min_lines: 50
    - path: "src/hooks/useStorageUpload.js"
      provides: "Hook for Firebase Storage upload with progress"
      min_lines: 30
    - path: "src/components/FileList.jsx"
      provides: "List of uploaded files with URLs"
      min_lines: 30
  key_links:
    - from: "Dropzone"
      to: "useStorageUpload"
      via: "hook call"
      pattern: "useStorageUpload|uploadFiles"
    - from: "Dropzone"
      to: "FileList"
      via: "component import"
      pattern: "FileList|setFiles"
    - from: "useStorageUpload"
      to: "Firebase Storage"
      via: "uploadBytes, getDownloadURL"
      pattern: "ref.*storage|uploadBytes"
---

<objective>
Create Dropzone for mass file upload to Firebase Storage.

Purpose: Enable admin drag-and-drop upload of images/PDFs to Firebase Storage. This replaces manual file uploads and enables "massivo" (massive) uploads as stated in Milestone 2 goal. Files are stored with proper path structure and download URLs are returned.

Output: Dropzone component, useStorageUpload hook, FileList display.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/firebase.js
@src/store/adminStore.js
@src/components/VolumeForm.jsx

# Firebase Storage Structure
- Storage path pattern: /volumes/{volumeId}/{filename}
- Use uploadBytes for raw file upload
- Use getDownloadURL to get public URL
- Use ref() to create storage reference

# UI Pattern
- Drag-and-drop zone with visual feedback (dashed border, highlight on dragover)
- Progress bar for each file during upload
- Multiple file support with queue
- Tailwind styling matching admin panel theme
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create useStorageUpload hook</name>
  <files>src/hooks/useStorageUpload.js</files>
  <behavior>
    - Hook accepts files array and storage path
    - Uploads each file to Firebase Storage
    - Returns progress for each file (0-100)
    - Returns download URLs after completion
    - Handles errors gracefully
  </behavior>
  <action>
Create storage upload hook in src/hooks/useStorageUpload.js:
- Import ref, uploadBytesResumable, getDownloadURL from firebase/storage
- Import getStorage from firebase/firestore (actually firebase/storage)
- Create function uploadFiles(files[], path) 
- For each file: create ref, uploadBytesResumable (for progress), getDownloadURL
- Track progress per file with on 'state_changed' observer
- Return { progress: {}, urls: {}, error: null }
- Export useStorageUpload hook
  </action>
  <verify>
    <automated>npm run lint -- --quiet src/hooks/useStorageUpload.js</automated>
  </verify>
  <done>Hook uploads files with progress tracking</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Create Dropzone component</name>
  <files>src/components/Dropzone.jsx</files>
  <behavior>
    - Drag-and-drop zone accepts files
    - Click to open file picker also works
    - Visual feedback on dragover (border color change)
    - Shows file list with upload button
    - Displays progress during upload
  </behavior>
  <action>
Create dropzone in src/components/Dropzone.jsx:
- Use useStorageUpload hook
- Create drop handlers: onDragEnter, onDragLeave, onDrop, onChange (file input)
- State: files[] for selected files, uploading boolean, progress object
- UI: large drop area with dashed border, gold on dragover
- File list shows each file name and size
- Upload button triggers uploadFiles(files, '/volumes/uploads')
- Progress bar per file during upload
- Show success with copyable download URL after completion
- Export default Dropzone component
  </action>
  <verify>
    <automated>npm run lint -- --quiet src/components/Dropzone.jsx</automated>
  </verify>
  <done>Dropzone accepts files, shows progress, returns URLs</done>
</task>

<task type="auto">
  <name>task 3: Create FileList component</name>
  <files>src/components/FileList.jsx</files>
  <action>
Create file list display in src/components/FileList.jsx:
- Props: files array with { name, url, size, uploadedAt }
- Render list of uploaded files with icons (file type)
- Copy button for each download URL
- Delete button to remove from Storage
- Use Tailwind: list items with hover states
- Export default FileList component
  </action>
  <verify>
    <automated>npm run lint -- --quiet src/components/FileList.jsx</automated>
  </verify>
  <done>FileList displays uploaded files with actions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→Firebase Storage | Admin writes storage bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | Large file upload | mitigate | "Limit file size to 50MB, validate type" |
| T-03-06 | Tampering | File injection | accept | "Admin users are trusted" |
</threat_model>

<verification>
Check that:
1. npm run lint passes for all new files
2. Drag-and-drop uploads files to Firebase Storage
3. Progress shown during upload
4. Download URLs returned after completion
</verification>

<success_criteria>
- [x] useStorageUpload uploads with progress
- [x] Dropzone accepts drag-and-drop and click
- [x] FileList shows uploaded files
- [x] URLs can be copied and used in VolumeForm
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-panel/03-03-SUMMARY.md`
</output>